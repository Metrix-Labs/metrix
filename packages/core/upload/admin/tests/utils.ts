import {
  render,
  renderHook,
  waitFor,
  act,
  fireEvent,
  screen,
  server,
} from '@metrixlabs/admin/strapi-admin/test';

export { render, renderHook, waitFor, server, act, fireEvent, screen };
